# 청년교구 주보 생성기 — 설계 문서

미리캔버스 수작업을 대체하는 웹 기반 주보 자동 조판 도구.
배경 이미지를 올리고 텍스트만 입력하면 자동으로 정렬·페이지 분할되어 PNG로 바로 받을 수 있다.

---

## 0. 현행 프로세스 → 이 도구에서의 대응

| # | 지금 하는 일 | 이 도구에서 |
|---|---|---|
| 1 | 미리캔버스에서 PC로 작업 | 웹에서 작업 (PC 최적화, 모바일은 조회용) |
| 2 | 밴드(오세예/청년선교)에서 **양동이 배경, 날짜 없는 버전** 다운로드 | 그 이미지를 업로드 → 891:1260 자동 크롭, 테마로 저장해 재사용 |
| 3 | 1p 표지 / 2p 예배시간 + 생일자(지우에게 받음) | **고정 페이지 관리**에 저장 → 매주 자동 첨부. 생일은 월별 명단 |
| 4 | 목사님이 **구글 문서로 올린 광고**(보통 전날/2부예배)를 보고 손으로 옮겨 배치, 제목은 노란 배경 | **문서 내용 통째로 붙여넣기 → 광고 블록 자동 파싱 + 노란 하이라이트 자동 적용 + 페이지 자동 분할** |
| 5 | 마지막 페이지에 남은 광고 + 설교 제목·성구 | 남은 광고는 자동으로 마지막 페이지에 흐르고, 설교 제목·성구 입력란은 항상 마지막에 고정 |
| 6 | 각 페이지 상단 날짜를 일일이 수정 | 날짜 **한 번만** 입력 → 전 페이지 자동 반영 |
| 7 | 1p 더피스 로고가 미리캔버스 프리미엄, 건당 약 1,100원 | 로고 이미지를 한 번 등록해두고 재사용 → **결제 불필요, 비용 0원** |
| 8 | 청년부 밴드 업로드 + 새가족부 QR용으로 김효정 님께 별도 전달 | 내보내기 완료 화면에 **배포 체크리스트** + ZIP·개별 PNG·공유 링크 제공 |

> 용어: 주보에는 `청년교구 소식`으로 표기되지만 실무에서는 **광고**라고 부른다. 이 문서에서는 UI 라벨을 "광고"로 통일한다.

---

## 1. 규격

| 항목 | 값 |
|---|---|
| 캔버스 | 891 × 1260 px (A4 세로 비율 1:1.414) |
| 내보내기 | @2x = 1782 × 2520 px PNG |
| 파일명 | `2026-07-19_1.png` … 순번 |

### 폰트 — 모두 `public/fonts/`에 포함, 상업 이용·웹 임베딩 허용
| 용도 | 폰트 |
|---|---|
| 주보 제목 | HS새마을체 |
| 주보 본문 | ACC어린이마음고운체 |
| 표지 글자 (기본) | 카페24 써라운드 — 두툼하고 끝이 둥근 느낌이 원본에 가장 가깝다 |
| 표지 글자 (대안) | 배민 주아 · 여기어때 잘난 |
| 화면 UI | Pretendard |

내보낼 때는 폰트를 data URL로 직접 임베딩한다. 라이브러리에 맡기면 브라우저 확장이 주입한
스타일시트를 훑다가 멈추는 경우가 있어 그 과정을 통째로 건너뛴다.

### 색상 (주보 이미지에서 추출)
| 용도 | 값 |
|---|---|
| 섹션 제목 | `#3B3B98` (남보라) |
| 본문 텍스트 | `#3D3D5C` |
| 블록 제목 하이라이트 | `#FFF3B0` (형광펜 노랑) |
| 본문 카드 | `#FFFFFF` / opacity 0.72, radius 24 |

---

## 2. 페이지 레이아웃 (공통)

```
┌─────────────────────────────────┐  891
│         2026.07.19.주일          │  y=40, center, 26px
│  ┌───────────────────────────┐  │  카드: x 88~803 (w 717)
│  │                           │  │        y 105~1130
│  │      청년교구 소식          │  │  섹션 제목 52px, y=195
│  │                           │  │
│  │      <블록 제목>            │  │  28px + 하이라이트
│  │   본문 본문 본문 본문        │  │  27px, line-height 1.6
│  │   본문 본문 본문            │  │  center 정렬
│  │                           │  │
│  │      <블록 제목>            │  │  블록 간격 48px
│  │   본문 본문 본문            │  │
│  │                           │  │
│  └───────────────────────────┘  │
│ 담당 목사  | 김준형(010-...)      │  y=1180, left x=30
│ 청년교구 계좌 | 토스뱅크 1000-...  │  y=1215
└─────────────────────────────────┘  1260
```

- 배경 이미지는 전면 `object-fit: cover`
- 날짜/푸터는 **모든 페이지에 자동 반복** (한 번만 입력)
- 푸터 2줄은 개인정보이므로 교회 설정에 저장, 매주 재입력 불필요

---

## 3. 페이지 구성 — 고정 페이지 / 주간 페이지 분리

주보는 성격이 다른 두 종류의 페이지로 나뉘며, **관리 기능도 완전히 분리**한다.

```
[ 고정 페이지 관리 ]  ← 메뉴 A. 가끔만 수정
   ① 표지 (포도나무교회 마크 + 청년부)
   ② 청년부 일정 (예배 안내 + N월 생일)
                    │
                    ├─ 저장된 고정 페이지
                    ▼
[ 주간 주보 작성 ]    ← 메뉴 B. 매주 사용
   날짜 입력 + ③소식 + ④주요일정 + ⑤본문 말씀
                    │
                    ▼
        고정 ①② + 주간 ③④⑤ 자동 결합 → 전체 주보 PNG
```

**메뉴 A · 고정 페이지 관리** — 예배 시간이 바뀌거나, 배경을 계절에 맞게 교체하거나, 매월 생일 명단을 갱신할 때만 들어감.
**메뉴 B · 주간 주보 작성** — 매주 실제 작업. 날짜와 소식만 입력하면 ①②는 자동으로 앞에 붙는다. 이 화면에서는 ①② 내용을 건드릴 수 없고 **미리보기로만 확인**(날짜만 자동 갱신).

---

### ① 표지 — 고정
아치형 교회명 + 퍼즐 로고 + `THE PIECE` / `청년교구` / `VINE` / `YOUNG ADULT & COLLEGE`.

**표지 글자는 이미지가 아니라 편집 가능한 서식으로 구현**했다. 기본 문구·위치·크기가 채워져 있어
문구만 고쳐 쓰면 되고, 줄 단위로 추가·삭제할 수 있다.

| 요소 | 형태 | 기본값 |
|---|---|---|
| 포도나무교회 | 아치 ∩ | y 84 · 96px · 휘어짐 138 · 자간 30 |
| 퍼즐 로고 | 이미지 | x 296 · y 325 · 너비 300 |
| THE PIECE | 아치 ∪ | y 880 · 62px · 휘어짐 −90 |
| 청년교구 | 아치 ∪ | y 936 · 88px · 휘어짐 −105 · 자간 34 |
| VINE / YOUNG ADULT & COLLEGE | 직선 | y 1176 / 1208 |

- 곡선 글자는 SVG `textPath`로 구현해 글자가 곡선을 따라 회전한다.
  휘어짐이 **양수면 ∩(위로), 음수면 ∪(아래로)** 휜다.
- **퍼즐 로고는 직접 그린 SVG**(`public/logo/the-piece.svg`)가 기본 포함되어 있다.
  네 조각이 실제로 맞물리는 구조(돌기·홈)이며, 교회 로고가 따로 있으면 업로드해 교체한다.
- 표지 서식이 바뀌면 `templateVersion`을 올려 예전 저장본을 새 기본 배치로 자동 갱신한다.

고정 페이지에서 수정 가능한 항목: 표지 전용 이미지, 로고 표시 여부·위치·크기,
글자별 문구·글꼴·크기·세로위치·자간·휘어짐·색, 날짜 표시 여부.

### ② 청년부 일정 — 고정
라벨-값 2단 정렬. 라벨은 가운데 거터 기준 **우측 정렬**, 값은 **좌측 정렬**.

```
      주일 1부 예배 │ 8:30
      주일 2부 예배 │ 11:00
        청년부 예배 │ 15:00
목요일 이사야62 기도회 │ 20:00 / 본당
          새벽 예배 │ 매일 6:30 / ZOOM
                   │ (ID: 485 079 2938,
                   │  PW: 1004)
```
- 값이 길어 줄바꿈되면 자동으로 라벨 아래 값 컬럼에 이어붙음
- 행 추가/삭제/순서변경 가능

하단 **`N월 생일`** 섹션 — 이름 목록. 개인정보이므로 로그인 사용자만 열람.
월이 바뀔 때만 갱신하면 되므로 고정 페이지 관리에 포함하되, **월별로 따로 저장**해서
주보 날짜의 월에 해당하는 명단이 자동 선택되게 한다.

```
birthdays: { "2026-07": ["홍길동", "김철수", ...],
             "2026-08": [...] }
```
주간 작성 화면에서 해당 월 명단이 비어 있으면 "8월 생일 명단이 비어 있습니다" 안내 표시.

---

### ③ 청년교구 소식 — 주간 ← 자동 분할의 핵심
블록 리스트. 블록 = `{ 제목, 본문 }` 이며 **절대 분리되지 않음**. 분량에 따라 페이지 수가 매주 달라진다.

### ④ 주요일정 — 주간
`행사명 + 날짜` 목록. 소제목 `주요일정`. 지난주에서 복사 후 지난 일정만 지우는 식으로 사용.

### ⑤ 본문 말씀 — 주간
`제목: …` / `본문: …` 2줄. ④와 한 페이지에 함께 들어가는 경우가 많아 **④+⑤ 합본 페이지** 지원.

---

## 4. 자동 조판 엔진

### 4-1. 블록 원자성 (keep-together)
```ts
type Block = {
  id: string
  title: string        // <주중 기도회>
  body: string         // 여러 줄
  pageBreakBefore?: boolean   // 강제 페이지 나누기
  keepWithNext?: boolean      // 다음 블록과 같은 페이지에 유지
}
```
제목과 본문은 한 덩어리로만 이동한다. 페이지 경계에서 잘리지 않는다.

### 4-2. 페이지 분할 알고리즘
1. 화면 밖 숨은 컨테이너(`position:absolute; visibility:hidden`)에 실제 스타일 그대로 블록 렌더
2. `getBoundingClientRect()`로 블록별 실제 높이 측정 (한글 줄바꿈까지 정확히 반영)
3. 콘텐츠 가용 높이 = 카드 높이 − 섹션 제목 − 상하 패딩 ≈ **880px**
4. 그리디로 채우다 초과하면 새 페이지 시작
5. 폰트/이미지 로드 완료 후 측정 (`document.fonts.ready` 대기)

```
남은높이 = 880
for 블록 in 블록들:
    if 블록.pageBreakBefore: 새 페이지
    if 블록높이 + 간격 > 남은높이:
        새 페이지; 남은높이 = 880
    배치; 남은높이 -= 블록높이 + 간격
```

### 4-3. 넘침 처리
| 상황 | 처리 |
|---|---|
| 블록이 살짝 넘침 (≤10%) | **자동 축소**: 폰트 27→25px, 최소 23px까지 (옵션, 기본 ON) |
| 블록 하나가 한 페이지보다 큼 | ⚠️ 경고 + 편집기에서 분할점 지정 UI 제공 |
| 마지막 페이지가 너무 비었음 | 정보 표시만 (자동 재배치 안 함 — 순서 보존이 우선) |

### 4-4. 세로 정렬
페이지별 `top` / `center` 선택. 기본값은 원본과 동일하게 `top`.

---

### 4-5. 광고 텍스트 붙여넣기 파서 ★ 실사용 시간 단축의 핵심
목사님이 구글 문서로 올린 광고 전문을 **통째로 복사·붙여넣기**하면 블록 배열로 자동 변환한다.

```
┌─ 광고 붙여넣기 ───────────────────┐
│ <주중 기도회>                     │
│ 수요기도회와 목요이사야62에        │
│ 적극적으로 참여해주시기 바랍니다.   │
│                                  │
│ <결혼>                           │
│ 김OO 자매님의 결혼식이 ...        │
└──────────────────────────────────┘
        [ 블록으로 변환 ]
                ↓
   블록1 <주중 기도회> / 블록2 <결혼>  → 편집 가능
```

파싱 규칙 (우선순위 순):
1. `<...>` `[...]` `【...】`로 감싼 줄 → **블록 제목** (노란 하이라이트 자동 적용)
2. 빈 줄 2개 이상 → 블록 구분자
3. `1.` `-` `*` `▶` 로 시작하는 줄 → 제목 후보로 제안
4. 어느 것도 안 맞으면 → 첫 줄을 제목, 나머지를 본문으로 가정 후 **사용자가 확인**

- 변환 결과를 확정 전에 미리 보여주고, 제목/본문 경계를 드래그로 조정 가능
- 구글 문서에서 복사할 때 딸려오는 서식·빈칸·유니코드 공백 자동 정리
- 파싱 후 §4-3 알고리즘이 바로 페이지를 나누므로, **붙여넣기 → 완성까지 한 번에**

### 4-6. 스타일 기본값 & 사용자 오버라이드 ★
**등록하면 곧바로 완성된 상태로 나오고, 마음에 안 드는 부분만 손댄다.**

#### 우선순위
```
테마 기본값  →  페이지 타입 기본값  →  블록/요소별 오버라이드
   (전역)          (③광고, ④일정 …)         (사용자가 직접 수정)
```
아래 단계가 위를 덮어쓴다. 오버라이드가 없으면 자동값이 그대로 적용되므로,
아무것도 건드리지 않아도 결과물이 완성된다.

#### 기본 타이포 스케일
| 요소 | 정렬 | 크기 | 행간 |
|---|---|---|---|
| 상단 날짜 | 가운데 | 26px | 1.4 |
| 섹션 제목 (청년교구 소식) | 가운데 | 52px | 1.3 |
| 블록 제목 `<…>` | 가운데 | 28px | 1.4 |
| 블록 본문 | 가운데 | 27px | 1.6 |
| 예배 안내 라벨 | 오른쪽 | 27px | 1.5 |
| 예배 안내 값 | 왼쪽 | 27px | 1.5 |
| 푸터 | 왼쪽 | 24px | 1.4 |

#### 조절 가능한 항목
| 범위 | 항목 |
|---|---|
| 전체(테마) | 기본 폰트 크기 배율, 색상, 카드 투명도·여백, 날짜 형식 |
| 페이지 | 세로 정렬(위/가운데), 카드 위치·크기, 배경 이미지 |
| 블록/요소 | 정렬(좌·중앙·우), 폰트 크기, 굵기, 행간, 자간, 색상, 위아래 여백, 하이라이트 on/off, 강제 페이지 나누기 |

#### 되돌리기
- 수정한 항목 옆에 **↺ 기본값으로** 버튼 (요소 단위 / 페이지 단위 / 전체 단위)
- 수정된 항목은 파란 점으로 표시해 "자동값과 다름"을 한눈에 파악

#### 자동 조판과의 관계
- 사용자가 폰트 크기·여백을 바꾸면 **높이를 다시 측정해 페이지를 재분할**한다. 오버라이드는 그대로 유지된다
- §4-4의 자동 축소는 **사용자가 크기를 직접 지정한 블록에는 적용하지 않는다** (수동 지정이 항상 우선)
- 결과적으로 넘칠 수 있으므로, 그런 블록에는 ⚠️ 표시로 알려준다

### 4-7. 마지막 페이지 규칙
설교 제목·성구는 **항상 마지막 페이지 하단**에 고정된다.
```
[마지막 페이지]
  ├─ 앞 페이지에서 넘어온 남은 광고 (있으면)
  ├─ 주요일정 (있으면)
  └─ 본문 말씀  제목: … / 본문: …    ← 항상 여기
```
남은 광고 + 설교 영역이 한 페이지를 넘치면 광고를 한 페이지 더 만들고, 설교는 새 마지막 페이지로 밀어낸다.

---

## 5. 화면 구성

화면은 두 단계로 나뉜다. 1단계는 "무엇을 다루는가", 2단계는 "그 안에서 어느 작업인가".

```
1단계   [ 주보 만들기 ]   [ 보관함 ]                    2026.08.02.주일  [저장]
          이번 주 주보      지난 주보들

2단계    전체 공통 | 고정 페이지 | 본문 작성 | 전체 보기
```

2단계 탭은 **주보에 들어가는 내용을 적용 범위 순서대로** 나눈 것이다.

| 탭 | 적용 범위 | 내용 |
|---|---|---|
| **전체 공통** | 모든 페이지 | 날짜(상단) · 배경 이미지(전면) · 교회 정보(하단 푸터) |
| **고정 페이지** | 앞 2장 | ① 표지(이미지·더피스 로고) ② 청년부 일정(예배 안내·월별 생일) |
| **본문 작성** | 3장부터 | 광고 붙여넣기·블록 편집 · 주요일정 · 본문 말씀 |
| **전체 보기** | 완성본 | 전체 페이지 순서대로 확인 + **이미지 내보내기** |

- 날짜와 저장 버튼은 상단 헤더에 두어 어느 탭에서든 같은 자리에 있다
- 내보내기는 **전체 보기** 최상단에 크게 배치 — 마지막 단계라 가장 눈에 띄어야 한다
- 2단계 인증이 붙으면 1단계에 **관리자**(승인·초대코드) 메뉴가 추가된다

### 설정 · 고정 페이지
```
┌──────────────────────┬─────────────────┐
│ [ 표지 ][청년부 일정][교회 정보]        │  ← 2단계 탭
├──────────────────────┼─────────────────┤
│  표지 이미지 [업로드]  │  ┌────┐ ┌────┐ │
│  더피스 로고 [업로드]  │  │ 1  │ │ 2  │ │  고정 페이지 2장을
│  로고 위치·크기        │  │표지│ │일정│ │  항상 함께 미리보기
│                      │  └────┘ └────┘ │
└──────────────────────┴─────────────────┘

청년부 일정 탭: 예배 안내 행 편집(+행 추가 / 순서 이동)
               월별 생일 명단 [2026-08 ▼] + 다른 달에서 복사
```
저장 시 **현재 고정 페이지**로 반영되며, 이후 작성하는 주보에 자동 적용.
과거 주보는 작성 시점의 스냅샷을 유지 (§7 참고).

### 주보 만들기 · 본문 작성
```
┌──────────────────────┬─────────────────┐
│  날짜  2026.08.02    │  본문 미리보기 · 2장│
│  배경  [밴드에서 받은  │                 │
│        이미지 업로드]  │   ┌────┐ ┌────┐ │
│                      │   │ 3  │ │ 4  │ │
│  📋 광고 붙여넣기 ────│   └────┘ └────┘ │
│  ⠿블록1 ⠿블록2 ⠿블록3 │                 │
│  주요일정             │  ⚠ 블록3이 한    │
│  본문 말씀(설교 제목·성구)│   페이지를 초과 │
└──────────────────────┴─────────────────┘
```
이 화면은 **본문에만 집중**한다. 고정 페이지는 설정에서 다루고, 완성본은 전체 보기에서 확인한다.
**요소를 클릭하면 우측에 스타일 인스펙터가 열린다** (§4-6)
```
┌─ 블록2 <결혼> ────────── ↺ 기본값으로 ┐
│ 정렬     [좌] [중앙•] [우]             │
│ 폰트크기  ── ●──────  27px  ● 수정됨   │
│ 행간     ──── ●────  1.6              │
│ 여백 위/아래  24 / 24                  │
│ 하이라이트  ☑ 제목 노란 배경           │
│ ☐ 이 블록 앞에서 페이지 나누기          │
└───────────────────────────────────────┘
```
- 수정하면 즉시 재조판되고 페이지 수도 다시 계산됨
- 아무것도 안 건드려도 완성본이 나오므로, 인스펙터는 **필요할 때만** 열면 됨

- **광고 붙여넣기**가 첫 단계 — 구글 문서 내용을 통째로 붙여넣으면 블록이 자동 생성 (§4-5)
- 좌측 타이핑 → 우측 즉시 재조판, 페이지 수 실시간 변동
- ①② 썸네일은 회색 배지로 "고정" 표시, 클릭하면 메뉴 A로 이동
- 블록 드래그로 순서 변경
- **지난주 주보 복사**: 날짜만 바꾸고 달라진 광고만 수정 → 실제 작업 5분 내 완료

### 내보내기 & 배포 체크리스트
내보내기를 마치면 배포처를 잊지 않도록 체크리스트를 띄운다.

```
✅ PNG 5장 생성 완료  (2026-07-19_1.png … _5.png)

  [ ZIP 다운로드 ]  [ 개별 다운로드 ]  [ 공유 링크 복사 ]

  배포 체크리스트
   ☐ 청년부 밴드 업로드
   ☐ 김효정 님께 전달 (새가족부 QR용)
```
- 공유 링크: 로그인 없이 열람 가능한 읽기 전용 페이지 → 링크만 보내면 되므로 파일 전달이 간편
- 체크 상태는 주보별로 저장되어 목록에서 배포 여부를 확인 가능

---

## 6. 인증 & 승인

### 초대코드 + 관리자 승인 (초대코드 24시간 유효)
```
관리자가 초대코드 발급 → 단톡방 공유 (24h 후 자동 만료)
   ↓
가입 신청 (이름, 이메일, 비번, 초대코드)
   ├─ 유효한 코드 → status = approved, 즉시 이용
   └─ 코드 없음/만료 → status = pending, 승인 대기 화면
                          ↓
                    관리자 승인/거절
```

| 역할 | 권한 |
|---|---|
| `admin` | 가입 승인, 초대코드 발급, 테마/교회정보 관리, 전체 주보 |
| `editor` | 주보 작성·수정·내보내기 |
| `pending` | 대기 화면만 |

초대코드: 8자리 랜덤, `expires_at = now + 24h`, 사용 횟수 제한 옵션, 만료/사용현황 대시보드 표시.

---

## 7. 데이터 모델

```sql
users          id, email, name, role, status, approved_by, approved_at, created_at
invite_codes   id, code, created_by, expires_at, max_uses, used_count, revoked

churches       id, name, pastor_line, account_line        -- 푸터 고정 정보
fixed_pages    id, type(cover|worship), content JSONB, updated_by, updated_at
                                                          -- 메뉴 A에서 관리하는 ①②
birthdays      id, year_month('2026-07'), names TEXT[]    -- 월별 생일 명단
themes         id, name, background_url, cover_url, logo_url, font_config, color_config

bulletins      id, service_date, theme_id, status(draft|published),
               fixed_snapshot JSONB,                      -- 작성 시점의 ①② 스냅샷
               distribution JSONB,                        -- {band:bool, hyojeong:bool}
               share_token, created_by, updated_at
sections       id, bulletin_id, type(ads|schedule|sermon), order, content JSONB
```

스타일 오버라이드는 별도 테이블 없이 각 계층의 JSONB 안에 함께 담는다.
```jsonc
// themes.font_config / color_config  → 전역 기본값
// sections.content                    → 페이지·블록 단위
{
  "pageStyle": { "verticalAlign": "top" },
  "blocks": [
    { "id": "b1", "title": "<결혼>", "body": "…",
      "style": { "fontSize": 25, "align": "left" }   // 지정한 항목만 저장
    }
  ]
}
```
- `style`에 **없는 항목은 자동 기본값**을 따르므로, "기본값으로 되돌리기"는 해당 키를 지우기만 하면 된다
- 나중에 기본 스케일을 바꾸면 손대지 않은 요소는 새 기본값을 따라가고, 직접 조정한 요소는 그대로 유지된다

- 광고 블록은 `content` JSONB에 배열로 저장
- **페이지 분할 결과는 저장하지 않고 렌더링 시 매번 재계산** → 나중에 폰트·여백을 바꿔도 과거 주보가 깨지지 않음
- `fixed_snapshot`: 예배 시간이 나중에 바뀌어도 **과거 주보를 다시 열었을 때 그때 모습 그대로** 보이게 함
- `logo_url`: 더피스 로고를 한 번 등록해두고 계속 재사용 (미리캔버스 프리미엄 결제 불필요)
- RLS로 승인된 사용자만 접근, `share_token` 경로만 비로그인 읽기 허용

---

## 8. 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript |
| 스타일 | Tailwind CSS |
| 인증/DB/스토리지 | Supabase |
| 이미지 내보내기 | `html-to-image` (@2x) + `jszip` |
| 배포 | Vercel |

배경 이미지 업로드 시 Supabase Storage 저장 + 891:1260 비율로 자동 크롭(사용자가 크롭 영역 조정 가능).

### 백엔드 = Supabase (별도 서버 불필요)

| 필요한 것 | Supabase 기능 |
|---|---|
| 회원가입·로그인·세션 | **Auth** (이메일+비밀번호) |
| 사용자/주보/고정페이지 데이터 | **Postgres** (§7 테이블) |
| 배경·로고 이미지 파일 | **Storage** (버킷 `backgrounds`, `logos`) |
| 승인·권한 제어 | **RLS 정책** — DB 레벨에서 차단하므로 프론트를 우회해도 안전 |
| 초대코드 검증, 승인 처리 | **Server Actions** (Next.js) 또는 **Edge Function** |

RLS 정책 골자
```sql
-- 승인된 사용자만 주보 읽기/쓰기
create policy "approved can read" on bulletins for select
  using (exists (select 1 from users
                 where id = auth.uid() and status = 'approved'));

-- 공유 링크는 토큰이 일치할 때만 비로그인 읽기 허용
create policy "public share" on bulletins for select
  using (share_token = current_setting('request.share_token', true));

-- 승인·초대코드 발급은 admin만
create policy "admin only" on invite_codes for all
  using (exists (select 1 from users
                 where id = auth.uid() and role = 'admin'));
```

필요한 것은 Supabase 프로젝트 하나(무료 티어로 충분)와 `.env.local`의 URL·anon key 두 개뿐입니다.
1단계는 Supabase 없이 로컬에서 동작하도록 만들고, 2단계에서 연결합니다.

---

## 9. 개발 단계

### 1단계 — 조판 엔진 ✅ 완료 (로그인 없이 바로 사용 가능)
- [x] Next.js 16 + Tailwind v4 셋업
- [x] 폰트 포함 — 제목 HS새마을체 / 본문 ACC어린이마음고운체 / UI Pretendard
- [x] 페이지 렌더러 (891×1260, 배경/날짜/카드/푸터)
- [x] 페이지 타입 ①표지 ②청년부 일정 ③광고 ④주요일정 ⑤설교
- [x] 높이 측정 + 페이지 자동 분할 엔진 (블록 원자성)
- [x] **광고 텍스트 붙여넣기 파서**
- [x] **스타일 기본값 + 요소별 오버라이드 인스펙터**
- [x] 4개 탭 구조(전체 공통 / 고정 페이지 / 본문 작성 / 전체 보기) + 실시간 미리보기
- [x] 배경·표지·로고 이미지 업로드 (IndexedDB 원본 보관)
- [x] 고화질 내보내기 — 2·3·4배, JPG/PNG 선택, 개별·ZIP
- [x] **저장·불러오기 보관함** — 지난 주보 열기·복사, 만든 이미지 다시 받기
- [x] 데스크탑 우선 + 태블릿·모바일 대응
→ **이 시점에서 미리캔버스 작업 + 로고 결제 대체 완료**

남은 항목(4단계로 미룸): 폰트 자동 축소, 블록 드래그 정렬, 긴 블록 분할점 지정 UI

### 2단계 — 인증 & 저장 (Supabase)
- [ ] Supabase 프로젝트 연동, 이메일 가입/로그인
- [ ] 초대코드(24h 만료) 발급·검증
- [ ] 승인 대기 화면 + 관리자 승인 페이지
- [ ] RLS 정책
- [ ] 주보 저장/불러오기/목록
- [ ] 배경 이미지 Storage 업로드

### 3단계 — 고정 페이지 & 운영 편의
- [ ] **고정 페이지 관리 (①표지 / ②청년부 일정) 분리 메뉴**
- [ ] 월별 생일 명단 관리
- [ ] 주보 작성 시 고정 페이지 자동 첨부 + 스냅샷 저장
- [ ] 지난주 주보 복사
- [ ] 테마(배경 세트 + 로고) 관리
- [ ] 교회 기본정보(푸터) 설정
- [ ] 배포 체크리스트 + 공유 링크

### 4단계 — 다듬기
- [ ] 블록 드래그 정렬
- [ ] 폰트 자동 축소 튜닝
- [ ] 긴 블록 분할점 지정 UI
- [ ] 파서 규칙 보완 (실제 구글 문서 샘플 기준)
- [ ] 모바일 조회 대응

---

## 10. 확인 필요 사항

1. ~~폰트 파일~~ ✅ 해결 — 두 폰트 모두 상업 이용·웹 임베딩이 허용된 무료 폰트라 `public/fonts/`에 포함했다.
2. **더피스 로고 이미지** — 배경 투명 PNG를 고정 페이지 > 표지에 올리면 이후 결제 없이 계속 재사용된다. (아직 미등록)
3. **양동이 배경 원본** — 밴드에 올라오는 날짜 없는 버전 중 해상도가 가장 큰 파일. 1782×2520 미만이면 화면에서 경고가 뜬다.
4. **구글 문서 광고 샘플** — 최근 2~3주치 원문을 주시면 파서 규칙을 실제 형식에 맞게 다듬을 수 있음. (없어도 §4-5 기본 규칙으로 동작)
5. **포도나무교회 마크** — 표지 이미지에 포함되어 있는지, 별도 파일이 필요한지
6. 개인정보(생일 명단, 목사 연락처, 계좌번호)는 2단계 로그인 적용 후 승인된 사용자만 열람·편집
