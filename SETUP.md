# Supabase 연결하기

로그인·가입 승인·공용 저장·교회 QR을 켜는 방법입니다.
처음부터 끝까지 **약 15분** 걸리고, 카드 등록 없이 무료 플랜으로 충분합니다.

> Supabase 화면은 종종 바뀝니다. 메뉴 이름이 조금 다르면 비슷한 이름을 찾으시면 됩니다.

---

## 지금 상태와 연결 후의 차이

| | 지금 (로컬 모드) | 연결 후 (서버 모드) |
|---|---|---|
| 로그인 | 없음 | 이메일 + 비밀번호 |
| 아무나 들어올 수 있나 | — | 관리자가 승인해야 들어옴 |
| 주보 저장 위치 | 내 브라우저 | 서버 (모두가 같은 자료를 봄) |
| 다른 기기에서 이어서 작업 | ❌ | ✅ |
| 교회 QR (교인이 폰으로 보기) | ❌ | ✅ |

연결 전에도 앱은 그대로 동작합니다. 환경변수를 넣는 순간 서버 모드로 바뀝니다.

---

## 1단계 · 프로젝트 만들기

1. [supabase.com](https://supabase.com) 접속 → **Start your project** → GitHub 계정 등으로 로그인
2. **New project** 클릭
3. 입력값
   - **Name**: `the-piece-bulletin` (아무 이름이나 됩니다)
   - **Database Password**: 아무 비밀번호나 만들고 **어딘가 적어두세요**
     (앱에서는 안 쓰지만 나중에 DB에 직접 접속할 때 필요합니다)
   - **Region**: **Northeast Asia (Seoul)** — 가까울수록 빠릅니다
   - **Plan**: Free
4. **Create new project** → 준비되는 데 1~2분 걸립니다

---

## 2단계 · 표(테이블)와 권한 만들기

왼쪽 메뉴 **SQL Editor** → **New query**

아래 파일을 **번호 순서대로 하나씩** 실행합니다.
파일을 열어 **전체 복사 → 붙여넣기 → 오른쪽 아래 Run**(또는 `Ctrl+Enter`).
`Success. No rows returned` 이 나오면 그 파일은 끝난 것입니다. 다음 파일은 **New query**를 다시 눌러 시작합니다.

| # | 파일 | 하는 일 |
|---|---|---|
| 001 | [`001_init.sql`](./supabase/migrations/001_init.sql) | 표·권한(RLS)·저장소를 만듭니다 |
| 002 | [`002_share.sql`](./supabase/migrations/002_share.sql) | 공유 링크로 주보를 볼 수 있게 합니다 |
| 003 | [`003_email_and_block.sql`](./supabase/migrations/003_email_and_block.sql) | 메일 인증 확인 · 계정 차단 |
| 004 | [`004_invite_required.sql`](./supabase/migrations/004_invite_required.sql) | 초대코드를 만들고 그것으로 승인되게 합니다 |
| 005 | [`005_owner.sql`](./supabase/migrations/005_owner.sql) | 가장 먼저 가입한 계정을 **최고 관리자**로 정합니다 |
| 006 | [`006_share_privacy.sql`](./supabase/migrations/006_share_privacy.sql) | 이미 저장한 주보에서 그 달이 아닌 생일 명단을 잘라냅니다 |
| 007 | [`007_publish.sql`](./supabase/migrations/007_publish.sql) | **교회 QR 하나**에 이번 주 주보를 올리는 구조 |
| 008 | [`008_admin_only_delete.sql`](./supabase/migrations/008_admin_only_delete.sql) | 주보 삭제는 관리자만 |
| 009 | [`009_sunday_only.sql`](./supabase/migrations/009_sunday_only.sql) | QR은 **주일에만** 열립니다 (주중에는 아무것도 안 보입니다) |
| 010 | [`010_code_after_verify.sql`](./supabase/migrations/010_code_after_verify.sql) | 초대코드를 **메일 인증 뒤에** 받습니다 |
| 011 | [`011_decision_log.sql`](./supabase/migrations/011_decision_log.sql) | 승인·거절·차단·역할 변경의 **이력**을 남깁니다 |
| 012 | [`012_image_window.sql`](./supabase/migrations/012_image_window.sql) | 주보 이미지도 **그날 그 주보만** 열립니다 |
| 013 | [`013_reject_is_final.sql`](./supabase/migrations/013_reject_is_final.sql) | 거절을 되돌리는 것은 관리자만 |
| 014 | [`014_table_grants.sql`](./supabase/migrations/014_table_grants.sql) | 로그인한 사람에게 표 권한을 엽니다 |
| 015 | [`015_web_copies.sql`](./supabase/migrations/015_web_copies.sql) | 폰에서 볼 **화면용 축소본**도 QR로 열어줍니다 |
| 016 | [`016_recent_bulletins.sql`](./supabase/migrations/016_recent_bulletins.sql) | QR 화면에서 **지난 주보**(이번 주 포함 5부)를 넘겨볼 수 있게 합니다 |

> **순서를 지켜야 합니다.** 뒤 파일이 앞 파일이 만든 것을 고쳐 쓰기 때문에,
> 건너뛰면 "그런 표가 없다" 같은 오류가 납니다.
>
> 이미 쓰고 계신 곳이라면 **아직 실행하지 않은 번호부터** 이어서 하시면 됩니다.
> 같은 파일을 두 번 실행해도 탈이 나지 않게 적혀 있습니다.

## 3단계 · 메일 인증번호 켜기

가입하려면 **자기 메일함에 온 6자리 인증번호**를 넣어야 하게 합니다.
남의 메일 주소로 신청하는 것을 막습니다.

가입에는 세 가지가 **모두** 있어야 합니다.

```
초대코드 + 이름·비밀번호 + 메일 인증번호  →  가입 완료
```

### ① 이메일 확인 켜기

왼쪽 **Authentication** → **Sign In / Providers**(또는 **Providers**) → **Email**
→ **Confirm email** 이 **켜져 있는지** 확인 → 꺼져 있으면 켜고 **Save**

### ② 메일 서식을 인증번호로 바꾸기

Supabase는 메일 서식에 무엇을 적어두느냐에 따라 **링크**를 보내기도 하고 **인증번호**를 보내기도 합니다.
기본 서식은 링크라서, 인증번호가 나가도록 바꿔야 합니다.

**Authentication** → **Emails** → **`Confirm signup`** 탭

제목(Subject)

```
THE PIECE 주보 가입 인증번호
```

본문(Message body)은 [`supabase/email-templates/confirm-signup.html`](./supabase/email-templates/confirm-signup.html)을
열어 **전체 복사 → 붙여넣기**합니다. (앱과 같은 색을 쓴 카드 모양입니다)

핵심은 링크(`{{ .ConfirmationURL }}`)를 빼고 **인증번호(`{{ .Token }}`)**를 넣는 것입니다.
**Save** 를 누르면 끝입니다.

> 비밀번호 재설정 메일(`Reset Password`)은 **그대로 두세요.** 그쪽은 링크가 맞습니다.

### ③ 메일 보내는 곳 연결 (SMTP)

Supabase가 기본으로 주는 발송기는 **한 시간에 2통**, 그것도 **프로젝트 팀원 주소로만** 갑니다.
교인들이 인증번호를 받으려면 반드시 연결해야 합니다.

가장 쉬운 방법은 **Gmail 앱 비밀번호**입니다. (도메인 없이 무료, 하루 500통)

1. [Google 계정 보안](https://myaccount.google.com/security) → **2단계 인증**을 켭니다
   (켜지 않으면 다음 단계가 보이지 않습니다)
2. [앱 비밀번호](https://myaccount.google.com/apppasswords) → 이름에 `주보` → **만들기**
   → 나오는 **16자리**를 복사합니다
3. Supabase → **Project Settings** → **Authentication** → **SMTP Settings**
   → **Enable custom SMTP** 켜기

   | 칸 | 넣을 값 |
   |---|---|
   | Sender email address | 본인 Gmail 주소 (2번에서 만든 계정과 같아야 합니다) |
   | Sender name | `THE PIECE 주보` |
   | Host | `smtp.gmail.com` |
   | Port number | `465` |
   | Minimum interval per user | `60` |
   | Username | 본인 **Gmail 주소** (프로젝트 이름이 아닙니다) |
   | Password | 복사한 **16자리 앱 비밀번호** (공백 빼고) |

4. **Save changes**
5. **Rate limits** 에서 시간당 발송 수를 `100` 정도로 올려두면 여러 명이 한꺼번에 가입해도 안 밀립니다

---

## 4단계 · 연결 키 복사

왼쪽 아래 **Project Settings**(톱니바퀴) → **API** (또는 **API Keys** / **Data API**)

두 가지를 복사합니다.

| 항목 | 생김새 | 쓰는 곳 |
|---|---|---|
| **Project URL** | `https://xxxxxxxx.supabase.co` | `NEXT_PUBLIC_SUPABASE_URL` |
| **anon public** | `eyJ...` 로 시작하는 긴 문자열 | `NEXT_PUBLIC_SUPABASE_ANON_KEY` |

> ⚠ 같은 화면에 **RESTful endpoint**(`https://xxxxxxxx.supabase.co/rest/v1`)도 함께 적혀 있습니다.
> 뒤에 `/rest/v1`이 붙은 쪽이 아니라 **`.supabase.co`에서 끝나는 주소**를 넣으세요.
> (잘못 넣어도 앱이 알아서 잘라내지만, 화면에 그대로 보여야 헷갈리지 않습니다.)

> ⚠ 같은 화면에 **service_role** 키도 있습니다. **이건 절대 쓰지 마세요.**
> 모든 권한을 우회하는 키이고, `NEXT_PUBLIC_`으로 시작하는 값은 브라우저에 그대로 노출됩니다.
> 실수로 어딘가 올렸다면 그 화면에서 바로 **Reset**하세요.

---

## 5단계 · Vercel에 등록 (배포본)

1. Vercel 프로젝트 → **Settings** → **Environment Variables**
2. 두 개를 추가합니다

   | Key | Value |
   |---|---|
   | `NEXT_PUBLIC_SUPABASE_URL` | 4단계에서 복사한 Project URL |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 4단계에서 복사한 anon public |
   | `GEMINI_API_KEY` (선택) | 광고 AI 자동 구분을 쓸 때만 |
   | `NEXT_PUBLIC_STORAGE_LIMIT_MB` (선택) | Supabase 플랜의 저장 한도(MB). 안 적으면 무료 플랜 기준 `1024` |

   > `NEXT_PUBLIC_STORAGE_LIMIT_MB`는 보관함의 **저장 공간** 칸이 "몇 부 더 저장할 수 있는지"를
   > 계산하는 데 씁니다. Storage API는 플랜 한도를 알려주지 않아 여기서 일러줘야 합니다.
   > Pro 플랜(100GB)이면 `102400`을 적으세요. 값이 틀려도 저장은 되지만 남은 양이 잘못 나옵니다.

   > `GEMINI_API_KEY`에는 `NEXT_PUBLIC_`을 **붙이지 마세요.** 붙이면 브라우저에 그대로 노출됩니다.
   > 이 키는 서버에서만 쓰이고, 서버 모드에서는 **승인된 사람만** 그 기능을 부를 수 있습니다.

3. **Environment**는 **Production · Preview · Development 모두 체크**
4. **Save**
5. **Deployments** 탭 → 맨 위 배포의 `⋯` → **Redeploy**

> 환경변수는 **다시 배포해야 반영됩니다.** 저장만 하면 바뀌지 않습니다.

---

## 6단계 · 배포 주소를 Supabase에 알려주기

비밀번호 재설정 메일의 링크는 **Supabase가 허락한 주소로만** 돌아옵니다.
배포 주소를 적어두지 않으면 링크를 눌러도 로그인 화면으로 튕깁니다.

Supabase → **Authentication** → **URL Configuration**

| 칸 | 넣을 값 |
|---|---|
| **Site URL** | `https://내-프로젝트.vercel.app` |
| **Redirect URLs** | `https://내-프로젝트.vercel.app/**` 그리고 `http://localhost:3000/**` |

`/**`는 그 주소 아래 모든 경로를 뜻합니다. 로컬에서도 재설정 메일을 시험하려면
`localhost` 줄까지 함께 넣어두세요.

---

## 7단계 · 내 컴퓨터에도 등록 (선택)

로컬에서도 서버 모드로 쓰려면 프로젝트 폴더에 `.env.local` 파일을 만듭니다.

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# 선택 — 광고 AI 자동 구분 (없으면 규칙 방식으로만 나눕니다)
GEMINI_API_KEY=여기에_발급받은_키

# 선택 — 플랜의 저장 한도(MB). 무료 1024 · Pro 102400. 안 적으면 1024로 봅니다
NEXT_PUBLIC_STORAGE_LIMIT_MB=1024
```

저장 후 개발 서버를 껐다 켜야 반영됩니다.

> `.env.local`은 `.gitignore`에 들어 있어 저장소에 올라가지 않습니다.

---

## 8단계 · 첫 가입 = 최고 관리자

배포된 주소로 들어가면 로그인 화면이 뜹니다.

1. **가입 신청** → 이름·이메일·비밀번호(8자 이상) 입력
2. 메일로 온 **6자리 인증번호**를 넣습니다
   (아직 아무도 없는 상태라 이때만 초대코드 칸이 나오지 않습니다)
3. **가장 먼저 가입한 사람이 최고 관리자**가 되고 바로 승인됩니다
4. 그러니 **반드시 본인이 먼저 가입**하세요

> 이미 가입을 마치셨다면 `005_owner.sql`이 그 계정을 최고 관리자로 잡아줍니다.

관리자가 되면 상단에 **관리자** 메뉴가 보입니다.

---

## 누가 무엇을 할 수 있나

| | 최고 관리자 (나) | 관리자 | 편집자 |
|---|---|---|---|
| 주보 작성·수정 | ✅ | ✅ | ✅ |
| 가입 승인·거절·차단 | ✅ | ✅ | ❌ |
| 초대코드 발급·폐기 | ✅ | ✅ | ❌ |
| 승인·거절 이력 보기 | ✅ | ✅ | ❌ |
| **관리자 임명·회수** | ✅ | ❌ | ❌ |

- 관리자는 여러 명을 둘 수 있습니다. **관리자로 올리고 내리는 것만** 최고 관리자의 몫입니다
- 다른 관리자는 **최고 관리자 계정을 고치거나 차단할 수 없습니다**
- 최고 관리자는 화면에서 바꿀 수 없습니다.
  옮겨야 한다면 **Table Editor → `users`** 에서 `is_owner` 를 직접 손봐야 합니다

---

## 다른 분들 들어오게 하기

가입은 이 순서로 갑니다.

```
이름 · 이메일 · 비밀번호  →  메일 인증  →  초대코드  →  가입 완료
                                             ↓ 코드가 없으면
                                          승인 대기 (관리자가 승인)
```

**관리자 → 초대코드 → + 코드 발급** → `복사` → 단톡방에 공유

- **24시간** 동안, **최대 10명**까지 쓸 수 있습니다
- 코드를 넣으면 승인을 기다리지 않고 바로 시작합니다
- 잘못 공유했다면 **폐기**를 누르면 즉시 막힙니다

> **초대코드는 메일 인증을 마친 뒤에 넣습니다.**
> 코드를 받으러 간 사이에 적어둔 것이 다 날아가지 않게 하려는 것입니다.
> 코드가 없어도 인증까지는 할 수 있고, 그 계정은 **승인 대기**로 남습니다 —
> 승인 전에는 아무것도 보지 못하고 아무것도 하지 못합니다.

### 승인 대기가 생기는 경우

코드 없이 신청했거나, 인증번호를 넣는 사이에 코드가 **만료되거나 다 쓰인** 경우입니다.
**관리자 → 사람**에서 이름·이메일을 보고 **승인** 또는 **거절**하시면 됩니다.

> 승인 전에는 주소를 직접 입력해도 아무것도 보이지 않습니다.
> 화면이 아니라 데이터베이스에서 막고 있어서, 우회할 방법이 없습니다.

### 거절한 뒤에 마음이 바뀌면

**관리자 → 사람 → `거절됨`** 을 눌러 그 사람을 찾고 **거절 취소하고 승인**을 누르면 됩니다.
거절된 사람은 초대코드로도 다시 들어오지 못합니다 — 되돌리는 것은 관리자만 합니다.

### 누가 왜 그렇게 정했는지

**관리자 → 이력** 에 승인·거절·차단·역할 변경이 모두 남습니다.
거절과 차단은 **사유를 적어야** 넘어가고, 그 사유는 관리자만 봅니다.
한 번 남은 기록은 아무도 고치거나 지울 수 없습니다.

---

## 교회 QR 쓰기

QR은 **한 장만 뽑아 입구에 붙여두면** 됩니다. 주소가 `https://내-주소/now` 하나로 고정이라
매주 새로 인쇄하지 않아도 됩니다.

```
주보 저장          →  아직 아무에게도 안 보입니다
전체 보기 → 교회 QR → [이 주보를 QR에 올리기]
                   →  같은 QR이 이 주보를 보여줍니다
```

- **저장은 공개가 아닙니다.** 올리기 전에는 QR을 찍어도 "아직 올라오지 않았습니다" 만 보입니다.
  작성 중인 주보가 새어 나가지 않습니다
- 올릴 때 **페이지 이미지를 만들어 함께 올립니다.** 교인 폰에서는 그 이미지가 보이므로
  인쇄물과 똑같고, 폰이 조판을 다시 계산하지 않아 바로 뜹니다
- 내용을 고쳤으면 **다시 올려야** 바뀝니다
- 지금 무엇이 올라가 있는지는 **전체 보기 → 교회 QR** 과 **보관함의 `QR에 올라감` 배지**로 봅니다
- 잘못 올렸으면 **내리기**를 누르면 즉시 닫힙니다. 주보와 QR 주소는 그대로 남습니다

> QR 이미지는 **1024px 이상**으로 받아 인쇄하세요. 한 번만 뽑으면 계속 씁니다.

## 로그인 유지

한 번 로그인하면 계속 로그인된 상태로 둡니다. 로그인 정보는 쿠키에 남고, 한 시간짜리 토큰이
만료되기 전에 자동으로 새로 받습니다. 창을 닫거나 컴퓨터를 껐다 켜도 이어집니다.

Supabase → **Authentication** → **Sessions** 에서 다음 두 칸은 **비워두세요.**
값을 넣으면 그 시간이 지날 때마다 로그인이 풀립니다.

| 칸 | 넣을 값 |
|---|---|
| **Time-box user sessions** | 비움 |
| **Inactivity timeout** | 비움 |

## 잘 됐는지 확인하기

- [ ] 배포 주소로 들어가면 **로그인 화면**이 뜬다 (예전처럼 바로 편집 화면이 뜨면 환경변수가 안 들어간 것)
- [ ] 가입하니 바로 들어가지고, 상단에 **관리자** 메뉴가 보인다
- [ ] 주보를 하나 저장하고 **보관함**에 뜬다
- [ ] **다른 브라우저(또는 폰)**에서 로그인해도 같은 주보가 보인다
- [ ] **전체 보기 → 교회 QR**에서 `이 주보를 QR에 올리기`를 누르면 "올라가 있습니다" 로 바뀐다
- [ ] 로그아웃한 브라우저(또는 폰)에서 `내-주소/now` 를 열면 주보가 이미지로 보인다

---

## 막힐 때

| 증상 | 원인과 해결 |
|---|---|
| 로그인 화면이 안 뜨고 바로 편집 화면 | 환경변수가 반영되지 않음. Vercel에서 **Redeploy** |
| 가입할 때 404 · `Invalid path specified in request URL` | `NEXT_PUBLIC_SUPABASE_URL`에 `/rest/v1`이 붙은 주소를 넣은 것입니다. `.supabase.co`까지만 남기고 **Redeploy** |
| 가입했는데 계속 승인 대기 화면 | 정상입니다. 관리자가 승인해야 합니다. 첫 사용자라면 `users` 표에 이미 다른 행이 있는지 확인하세요 |
| 첫 가입인데 관리자가 안 됨 | 이미 누가 먼저 가입한 것입니다. Table Editor > `users`에서 본인 행의 `role`을 `admin`, `status`를 `approved`로 바꾸면 됩니다 |
| 저장이 안 되고 오류가 뜸 | 2단계 SQL을 안 돌렸거나 일부만 돌아간 경우입니다. `001`부터 `006`까지 순서대로 다시 실행하세요 |
| QR로 연 주보에서 배경이 안 보임 | `002_share.sql`을 안 돌린 경우입니다 |
| QR을 찍으니 "아직 올라오지 않았습니다" | 저장만 하고 **올리기**를 안 누른 것입니다. 전체 보기 → 교회 QR에서 올려주세요 |
| QR을 찍으니 로그인 화면이 뜸 | 배포본이 옛 버전입니다. 최신 코드로 다시 배포하세요 |
| 고친 내용이 QR에 안 보임 | 고친 뒤에는 **다시 올리기**를 눌러야 바뀝니다 |
| 자꾸 로그아웃됨 | Supabase → Authentication → Sessions 의 **Time-box user sessions**·**Inactivity timeout** 을 비워두세요 |
| 메일에 숫자가 아니라 **링크**가 옴 | 3단계 ②를 안 한 것입니다. `Confirm signup` 서식에 `{{ .Token }}`을 넣으세요 |
| 인증번호 메일이 아예 안 옴 | 3단계 ③ SMTP를 연결하지 않으면 팀원 주소로만, 그것도 시간당 2통만 갑니다 |
| "인증번호가 맞지 않거나 시간이 지났습니다" | 번호는 1시간짜리입니다. **인증번호 다시 보내기**로 새로 받으세요 |
| 가입할 때 "초대코드가 유효하지 않거나 24시간이 지났습니다" | 코드가 만료·폐기됐거나 10명을 다 채운 것입니다. 관리자에게 새 코드를 받으세요 |
| 초대코드 칸이 잠겨 있음 | 코드는 **메일 인증을 마친 뒤에** 넣습니다. 인증을 먼저 끝내면 칸이 열립니다 |
| "이 계정은 가입이 거절되었습니다" | 관리자가 신청을 받지 않은 계정입니다. 코드로는 열리지 않으니, 관리자가 **사람 → `거절됨` → 거절 취소하고 승인**을 눌러야 합니다 |
| 관리자 화면에 **이력** 탭이 비어 있고 "표가 아직 없습니다" | `011_decision_log.sql` 을 실행하지 않은 것입니다 (2단계 표 참고) |
| 주일에 QR을 찍었는데 **주보 그림이 안 뜸** | `012_image_window.sql` 뒤로 이미지는 **지금 올라간 주보의 것만** 열립니다. 관리자 화면에서 그 주보가 실제로 올라가 있는지 확인하세요 |
| 로그인하니 인증번호 화면이 뜸 | 가입은 됐지만 메일 인증이 안 끝난 계정입니다. 그 자리에서 번호를 넣으면 됩니다 |
| **가입 신청을 눌러도 인증번호 칸이 안 뜨고 오류만 뜸** | 인증번호 칸은 메일이 나간 뒤에만 뜹니다. 대개 메일 발송이 막힌 것입니다 — 3단계 ③ SMTP를 확인하고, Supabase → **Logs** → **Auth**에서 그 시각의 오류를 보세요 |
| 비밀번호를 바꾸고 싶음 | 로그인한 상태에서 **오른쪽 위 자기 이름**을 누르면 비밀번호 바꾸기 화면이 나옵니다 (지금 비밀번호를 한 번 더 물어봅니다) |
| 관리자 화면에서 **승인** 버튼이 눌리지 않음 | 그 사람이 아직 메일 인증을 안 한 것입니다. **메일 인증 전** 배지를 확인하세요 |
| 역할 칸(편집자·관리자)이 회색으로 잠겨 있음 | 최고 관리자만 바꿀 수 있습니다. 최고 관리자 계정으로 로그인하세요 |
| 차단한 사람이 아직 들어가 있는 것 같음 | 차단 즉시 주보·설정에 손댈 수 없습니다. 열려 있던 화면은 새로고침하면 중지 안내로 바뀝니다 |
| 비밀번호 재설정 메일의 링크를 눌러도 로그인 화면으로 돌아옴 | 6단계 **Redirect URLs**에 배포 주소를 안 넣은 것입니다 |
| 광고 AI 자동 구분이 안 됨 | `GEMINI_API_KEY`가 없거나(규칙 방식으로 나뉩니다), 로그인·승인 전입니다 |

---

## 알아두면 좋은 것

- **무료 플랜 용량**: 저장소 1GB, DB 500MB. 주보 이미지가 장당 2~15MB이니 넉넉합니다.
  다만 몇 년 쌓이면 **보관함에서 오래된 주보를 지워** 정리하시면 됩니다
- **2주 이상 아무도 안 들어가면** 무료 프로젝트가 잠시 멈출 수 있습니다.
  대시보드에서 **Restore**를 누르면 바로 돌아옵니다
- **주보 이미지 접근**: 공유 QR로 로그인 없이 봐야 해서, 파일 주소를 아는 사람은 볼 수 있습니다.
  주소가 무작위라 찾아낼 수는 없지만, 생일 명단·연락처가 들어가니 QR을 공개된 곳에 붙일 때는
  감안해주세요
- **생일 명단**: 주보에 담기는 것은 **그 주보 달의 명단 하나뿐**입니다. 나머지 달은 설정에만 남아
  승인된 사람에게만 보입니다. 태어난 해는 붙여넣기에서 읽는 즉시 버려 어디에도 저장하지 않습니다
